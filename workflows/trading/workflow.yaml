name: trading
secrets-path: "../secrets.yaml"

staging-settings:
  user-workflow:
    workflow-name: "alpha-street-trading-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"
